<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @user.name %>
</p>

<p>
  <strong>Email:</strong>
  <%= @user.email %>
</p>

<p>
  <strong>First Micropost</strong>
  <% if @user.microposts.empty? %>
    Sorry, this user doesn*t seeem to have posted anything yet
  <% else %>
    <%= @user.microposts.first.content %>
  <% end %>
</p>

<%= link_to 'Edit', edit_user_path(@user) %> |
<%= link_to 'Back', users_path %>
